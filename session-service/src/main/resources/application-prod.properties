spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

spring.data.redis.host=${REDIS_HOST}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:}

spring.rabbitmq.host=${RABBITMQ_HOST}
spring.rabbitmq.port=${RABBITMQ_PORT:5672}
spring.rabbitmq.username=${RABBITMQ_USER}
spring.rabbitmq.password=${RABBITMQ_PASSWORD}

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

bidcast.selection-service.url=${BIDCAST_SELECTION_SERVICE_URL:http://selection-service:8084}
bidcast.session.presence.heartbeat-interval=${BIDCAST_SESSION_HEARTBEAT_INTERVAL:15s}
bidcast.session.presence.stale-after=${BIDCAST_SESSION_STALE_AFTER:60s}
bidcast.session.presence.close-empty-after=${BIDCAST_SESSION_CLOSE_EMPTY_AFTER:5m}
bidcast.session.presence.cleanup-interval=${BIDCAST_SESSION_CLEANUP_INTERVAL:30s}
